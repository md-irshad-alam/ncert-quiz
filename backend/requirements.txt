# Core API Framework
fastapi
uvicorn[standard]

# Database ORM
sqlmodel
SQLAlchemy
alembic

# Authentication & Security
bcrypt
python-jose[cryptography]
cryptography

# Request Parsing
python-multipart

# Settings & Config
pydantic-settings
python-dotenv

# AI - OpenRouter (OpenAI-compatible REST API, no extra SDK needed — uses httpx)
httpx

# Rate Limiting
slowapi

# Database Driver (PostgreSQL - for production)
psycopg2-binary==2.9.11

# HTTP requests
requests
